// Test complet MiniAlgo
PROGRAM Test
DECL
// Déclarations de variables simples
INTEGER : A, B, val_1 ;
FLOAT : X, Y ;

// Déclarations de tableaux
INTEGER : T[10] ;
FLOAT : F[5] ;

// Constantes
CONST PI = 3.14 ;        // réelle positive simple
CONST MAX = 100 ;        // entière positive simple
CONST MIN = (-32768) ;   // entière négative signée entre parenthèses
CONST TEMP = (+2.7) ;    // réelle positive signée entre parenthèses
CONST VAL = (0) ;        // entière signée zéro
ENDDECL
BEGIN
// Affectations simples
A = 10 ;
B = (-20) ;
val_1 = A + B ;

// Affectations avec réels
X = 3.14 ;
Y = (-2.71) ;

// Affectation avec tableaux
T[0] = (5) ;
F[2] = 3.0 ;

// Instruction conditionnelle
IF (A > B) {
    MAX = A ;
} ELSE {
    MAX = B ;
}

// Boucle FOR
FOR (i : 0 : 1 : 5) {
    A = A + i ;
}

// Boucle WHILE
WHILE (A < 20) {
    A = A + 1 ;
}

// Lecture et écriture
READ(B) ;
WRITE(MAX) ;

/* Commentaire
   multi-lignes */
END